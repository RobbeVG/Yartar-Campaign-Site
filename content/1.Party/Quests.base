filters:
  and:
    - and:
        - '!file.inFolder("templates")'
        - '!file.inFolder("0.DM Notes")'
    - file.hasTag("Quest")
views:
  - type: cards
    name: Active
    filters:
      and:
        - file.hasProperty("Status")
        - Status == "Active"
    cardSize: 300
